within Modelica.Clocked.UsersGuide.ReleaseNotes;
class Version_0_92 "版本 0.92 (Sept. 19, 2013)"
  extends Modelica.Icons.ReleaseNotes;
  annotation (Documentation(info="<html>
<p>
0.92 版向后兼容 0.91 版
</p>

<p>
与 0.91 版相比的改动：
</p>

<ul>
<li> 该库使用最新的 Modelica 标准库 (MSL) 3.2.1 版（但仍可使用 3.2 版）。</li>
<li> 添加了几个用于实数、整数和布尔信号的方便源块，它们与 MSL 中的块相似，但输出为时钟信号。</li>
<li> 添加了几个以时钟刻度而非模拟时间为参数的源块。</li>
<li> 所有新模块都至少用于一次测试。</li>
</ul>
</html>"));
end Version_0_92;