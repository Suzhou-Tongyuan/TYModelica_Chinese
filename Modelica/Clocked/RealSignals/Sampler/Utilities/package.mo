within Modelica.Clocked.RealSignals.Sampler;
package Utilities "通常不直接使用的工具组件"
  extends Modelica.Icons.UtilitiesPackage;

  annotation(Documentation(info = "<html>
<p>
该包包含的是通常不直接使用的工具组件，
但作为构建\"更高层次\"模块的构建块。
</p>
</html>"));
end Utilities;